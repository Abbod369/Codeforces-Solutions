//https://codeforces.com/contest/1312/problem/B
// Abbod369 - Latakia University..
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main()
{
    int t;
    int n , num;
    cin >> t;
    while(t--)
    {
        cin >> n;
        vector <int> vec(n);

        for(int i = 0; i < n; i++)
        cin >> vec[i];

        sort(vec.rbegin() , vec.rend());

        for(int i = 0; i < n; i++)
        cout << vec[i] << ' ';

        cout << endl;
    }

    return 0;
}
